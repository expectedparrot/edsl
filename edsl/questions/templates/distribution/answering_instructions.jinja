Return a single JSON object mapping every displayed label to its probability.
Use the exact labels, include zero-probability entries, and do not repeat keys.
Each probability must be a number from 0 to 1. Their sum must be 1.
Use probabilities, not percentages. Do not wrap the object in an "answer" field.
Example of the required format: {{ example_answer }}
{% if include_comment %}
After the JSON object, you may explain your answer on a new line starting with COMMENT:.
{% endif %}
