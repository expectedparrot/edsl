# Language semantics

The canonical manual is now part of the Mintlify documentation:

- [Read it on the documentation site](https://docs.expectedparrot.com/en/latest/shared-state/semantics).
- [Edit the canonical MDX source](../en/latest/shared-state/semantics.mdx).

Maintain the MDX chapters, not this relocation notice. Any older PDF or LaTeX
copy in this directory is an archived snapshot, not the current manual.

The machine schema, `test-vectors/`, and `run_vectors.py` remain here as
executable specification artifacts. The prose specification lives in Mintlify.
